\documentclass[10pt,a4paper]{article}

% Tight margins to ensure everything (even the Black Hole sim) fits on 1 page
\usepackage[margin=0.5in]{geometry}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{titlesec}

% Reverting to the original section style: Uppercase with a horizontal rule
\titleformat{\section}{\large\bfseries\uppercase}{}{0em}{}[\titlerule]
\titlespacing*{\section}{0pt}{1.2ex plus 1ex minus .2ex}{0.8ex plus .2ex}

% Clean links without distracting boxes
\hypersetup{
    colorlinks=true,
    urlcolor=blue,
    linkcolor=black
}

% --- JINJA DUMMY MACROS ---
% These allow the template to compile safely in Overleaf before Python injects the data
\providecommand{\VAR}[1]{}
\providecommand{\BLOCK}[1]{}

\begin{document}
\pagestyle{empty}

% --- HEADER ---
\begin{center}
    {\huge \textbf{\VAR{name}}} \\ \vspace{4pt}
    \VAR{phone} | \href{mailto:\VAR{email}}{\VAR{email}} | \href{\VAR{github}}{GitHub} | \href{\VAR{linkedin}}{LinkedIn} | \VAR{location}
\end{center}
\vspace{2pt}

% --- SUMMARY ---
\section*{Summary}
\VAR{summary}
\vspace{2pt}

% --- EDUCATION ---
\section*{Education}
\BLOCK{for edu in education}
\noindent \textbf{\VAR{edu.school}} \hfill \VAR{edu.location} \\
\textit{\VAR{edu.degree}} \hfill \VAR{edu.dates}
\vspace{2pt}
\BLOCK{endfor}

% --- HONORS & AWARDS ---
\BLOCK{if awards and awards|length > 0}
\section*{Honors \& Awards}
\BLOCK{for award in awards}
\noindent \textbf{\VAR{award.title}} --- \textit{\VAR{award.event}} \hfill \VAR{award.year}
\BLOCK{if award.bullets and award.bullets|length > 0}
\begin{itemize}[leftmargin=1.5em, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for bullet in award.bullets}
    \item \VAR{bullet}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}
\vspace{2pt}
\BLOCK{endfor}
\BLOCK{endif}

% --- EXPERIENCE ---
\section*{Experience}
\BLOCK{for job in experience}
\noindent \textbf{\VAR{job.company}} \hfill \VAR{job.location} \\
\textit{\VAR{job.title}} \hfill \VAR{job.dates}
\BLOCK{if job.bullets and job.bullets|length > 0}
% Optimized padding: Not completely squished (like nosep), but tight enough to save space
\begin{itemize}[leftmargin=1.5em, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for bullet in job.bullets}
    \item \VAR{bullet}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}
\vspace{2pt}
\BLOCK{endfor}

% --- PROJECTS ---
\section*{Projects}
\BLOCK{for proj in projects}
\noindent \textbf{\VAR{proj.name}} | \textit{\VAR{proj.tech_stack}}
\BLOCK{if proj.link and "http" in proj.link}\hfill \href{\VAR{proj.link}}{Link}\BLOCK{endif}

\BLOCK{if proj.bullets and proj.bullets|length > 0}
\begin{itemize}[leftmargin=1.5em, itemsep=1pt, parsep=0pt, topsep=2pt]
    \BLOCK{for bullet in proj.bullets}
    \item \VAR{bullet}
    \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}
\vspace{2pt}
\BLOCK{endfor}

% --- TECHNICAL SKILLS ---
\section*{Technical Skills}
\begin{itemize}[leftmargin=1.5em, itemsep=2pt, parsep=0pt, topsep=2pt]
    \BLOCK{for skill_category, skills in technical_skills.items()}
    \item \textbf{\VAR{skill_category | escape_latex}}: \VAR{skills}
    \BLOCK{endfor}
\end{itemize}

\end{document}